# Test Cases - Sub-Administrative Zone Management Module

## Test Case Documentation

| Test ID | Module / Feature | Objective | Expected Results | Status (P/F/B) |
|---------|------------------|-----------|------------------|----------------|
| TC1-501 | Load Sub-Administrative Zones | Verify that sub-administrative zones are loaded and displayed correctly based on selected dzongkhag and administrative zone | 1. Sub-Administrative Zone Management page loads successfully <br> 2. Dzongkhag dropdown displays all available dzongkhags <br> 3. Administrative Zone dropdown displays zones for selected dzongkhag <br> 4. First dzongkhag and administrative zone are auto-selected by default <br> 5. Sub-administrative zones table displays zones for selected filters with columns: ID, Name, Type, Area Code, Actions <br> 6. Map displays sub-administrative zone boundaries <br> 7. Loading indicator shows during data fetch <br> 8. Error message displays if loading fails | P |
| TC1-502 | Select Dzongkhag and Administrative Zone | Verify that selecting dzongkhag and administrative zone filters and loads corresponding sub-administrative zones | 1. Dzongkhag dropdown is visible and functional <br> 2. Administrative Zone dropdown is visible and functional <br> 3. Selecting a different dzongkhag updates administrative zone dropdown options <br> 4. Selecting a different administrative zone updates the sub-administrative zones list <br> 5. Map refreshes to show boundaries for selected filters <br> 6. Selected filters are saved and persist <br> 7. Sub-administrative zone selection is reset when filters change | P |
| TC1-503 | View Sub-Administrative Zone | Verify that admin can view sub-administrative zone details and data | 1. Clicking "View Details" button navigates to sub-administrative zone data viewer page <br> 2. Clicking on map feature displays popup with zone information (name, type, area code) <br> 3. Selecting zone from table highlights it in the map <br> 4. Map popup shows download options (GeoJSON, KML) <br> 5. Zone labels are displayed on map with name and area code | P |
| TC1-504 | Create Sub-Administrative Zone | Verify that admin can create a new sub-administrative zone successfully | 1. "Add Sub-Administrative Zone" button opens "Add New Sub-Administrative Zone" dialog <br> 2. Dialog displays form fields: Name (required, min 2 chars), Area Code (optional), Administrative Zone (required, pre-selected), Type (required) <br> 3. Form validation works for required fields and format <br> 4. Submitting valid form creates sub-administrative zone <br> 5. Success message displays: "Sub-administrative zone created successfully" <br> 6. Dialog closes and table refreshes with new zone <br> 7. Map refreshes to show new zone boundary <br> 8. Error messages display for validation failures or server errors | P |
| TC1-505 | Edit Sub-Administrative Zone | Verify that admin can update sub-administrative zone information | 1. Clicking "Edit" button on zone row opens "Edit Sub-Administrative Zone" dialog <br> 2. Dialog pre-fills with current zone data: Name, Area Code, Administrative Zone, Type <br> 3. Form validation works for editable fields <br> 4. Submitting with changes updates zone successfully <br> 5. Success message displays: "Sub-administrative zone updated successfully" <br> 6. Dialog closes and table refreshes with updated data <br> 7. Map refreshes to reflect changes <br> 8. Error messages display for validation failures or server errors | P |
| TC1-506 | Delete Sub-Administrative Zone | Verify that admin can delete a sub-administrative zone | 1. Clicking "Delete" button on zone row opens confirmation dialog <br> 2. Confirmation dialog displays: "Are you sure you want to delete [Name]?" with warning message <br> 3. Confirmation dialog has "Cancel" and "Delete" buttons <br> 4. Clicking "Delete" removes sub-administrative zone <br> 5. Success message displays: "Sub-administrative zone deleted successfully" <br> 6. Zone is removed from table and map <br> 7. Clicking "Cancel" closes dialog without deletion <br> 8. Error messages display if deletion fails | P |
| TC1-507 | Upload GeoJSON | Verify that admin can upload GeoJSON file for a sub-administrative zone | 1. Clicking "Upload GeoJSON" button opens "Upload GeoJSON" dialog <br> 2. Dialog displays selected zone information and file upload field <br> 3. File upload accepts .json and .geojson files (max 10MB) <br> 4. File validation works: shows error for invalid file types <br> 5. Submitting valid file uploads GeoJSON successfully <br> 6. Success message displays: "GeoJSON uploaded successfully" <br> 7. Dialog closes and map refreshes with new boundaries <br> 8. Error messages display for invalid files or upload failures | P |
| TC1-508 | Bulk Upload Sub-Administrative Zones | Verify that admin can bulk upload multiple sub-administrative zones via CSV/Excel file | 1. "Bulk Upload" button opens bulk upload dialog <br> 2. Dialog requires administrative zone selection and file upload <br> 3. File upload accepts supported formats (CSV/Excel) <br> 4. Upload process shows progress indicator <br> 5. Upload results display: success count, failure count, and error details <br> 6. Successfully uploaded zones appear in table <br> 7. Error messages display for invalid files or upload failures | P |
| TC1-509 | Bulk Upload Enumeration Areas | Verify that admin can bulk upload enumeration areas for a sub-administrative zone | 1. "Bulk Upload EA" button opens bulk upload enumeration areas dialog <br> 2. Dialog requires sub-administrative zone selection and file upload <br> 3. File upload accepts supported formats <br> 4. Upload process shows progress indicator <br> 5. Upload results display success and failure counts <br> 6. Successfully uploaded enumeration areas are created <br> 7. Error messages display for invalid files or upload failures | P |
| TC1-510 | Search Sub-Administrative Zones | Verify that search functionality filters zones correctly | 1. Search input field accepts text input <br> 2. Search filters results by ID, name, or area code <br> 3. Clear search button (X) appears when search has value <br> 4. Clearing search resets table to show all zones for selected filters <br> 5. Search is case-insensitive and supports partial matches | P |
| TC1-511 | Map View | Verify that map view displays correctly with sub-administrative zone boundaries | 1. Map loads and displays in the right panel <br> 2. Sub-administrative zone boundaries are rendered on the map with proper colors <br> 3. Map is centered on selected administrative zone with appropriate zoom level <br> 4. Zone labels display name and area code on map <br> 5. Loading overlay displays while map data is being fetched <br> 6. Map container is properly sized and responsive | P |
| TC1-512 | Download GeoJSON from Map | Verify that admin can download GeoJSON file for a sub-administrative zone from map popup | 1. Clicking on a sub-administrative zone feature on the map opens a popup <br> 2. Popup displays zone name, type, area code, and download buttons <br> 3. Clicking "GeoJSON" button in popup downloads the feature as GeoJSON file <br> 4. Downloaded file has format: "[zone_name].geojson" <br> 5. Success message displays: "GeoJSON file downloaded successfully" <br> 6. File contains FeatureCollection with the selected zone feature | P |
| TC1-513 | Download KML from Map | Verify that admin can download KML file for a sub-administrative zone from map popup | 1. Clicking on a sub-administrative zone feature on the map opens a popup <br> 2. Popup displays "KML" download button <br> 3. Clicking "KML" button in popup downloads the feature as KML file <br> 4. Downloaded file has format: "[zone_name].kml" <br> 5. Success message displays: "KML file downloaded successfully" <br> 6. File contains KML format with the selected zone feature | P |
| TC1-514 | Basemap Selection | Verify that basemap selector allows switching between different map styles | 1. Basemap selector dropdown is visible in map header <br> 2. Dropdown displays basemap categories and options (e.g., Positron, OpenStreetMap, Satellite) <br> 3. Selecting a different basemap updates the map background layer <br> 4. Zone boundaries remain visible after basemap change <br> 5. Selected basemap persists during the session <br> 6. Default basemap is "Positron" | P |
| TC1-515 | Layer Toggles | Verify that layer toggles show/hide different map layers correctly | 1. Layer toggle controls are visible (Sub-Administrative Zones, Administrative Zones, Dzongkhag) <br> 2. Toggling "Sub-Administrative Zones" layer shows/hides SAZ boundaries <br> 3. Toggling "Administrative Zones" layer shows/hides administrative zone boundaries <br> 4. Toggling "Dzongkhag" layer shows/hides dzongkhag boundaries <br> 5. Multiple layers can be displayed simultaneously <br> 6. Layer states persist during session | P |
| TC1-516 | Map Interaction | Verify that map interactions work correctly | 1. Clicking on map feature selects corresponding zone in table <br> 2. Hovering over map features highlights them with increased opacity <br> 3. Map popup displays zone information and download options <br> 4. Tooltip shows zone name and area code on hover <br> 5. Reload map button refreshes map data and boundaries <br> 6. Map interactions do not interfere with table selection | P |

## Test Status Legend
- **P** = Pass (Test case is ready for execution)
- **F** = Fail (Test case needs revision or has issues)
- **B** = Blocked (Test case cannot be executed due to dependencies)

